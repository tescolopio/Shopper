# Shopper
a web-based shop generator that pulls standardized, open-source data (using the Open5e API) and exports the final shop inventory as a perfectly formatted JSON schema. The GM can then drag and drop this JSON directly into Foundry VTT to instantly spawn a fully functional, digital merchant actor.
